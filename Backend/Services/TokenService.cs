// TokenService removed.
// AuthService now provides token generation and consumes JwtOptions via IOptions<JwtOptions>.
// Keeping this file as a placeholder to avoid build issues in some environments.

namespace Backend.Services;

// Intentionally empty. Token generation moved to AuthService.
